---
type Props = {
  title: string;
  description?: string;
};

const { title, description } = Astro.props;
---

<div class="mt-6 mb-2">
  <h1 class="text-md font-bold text-primary">{title}</h1>
  {description && <p class="text-muted-foreground text-sm">{description}</p>}
</div>
